from __future__ import annotations


async def fetch_rendered_page(url: str) -> str:
    # Placeholder: only allowed for public JS pages, never for CAPTCHA/login bypass.
    return f"PLAYWRIGHT_PLACEHOLDER::{url}"
